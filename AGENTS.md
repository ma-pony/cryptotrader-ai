# 项目协作与设计原则

本文约束后续设计与实现，不代表当前代码已经满足全部要求。发现偏差时，应核对源码，在任务范围内修正；超出范围的问题先报告，不擅自扩大实施范围。

## 核心原则

- 原则零：发挥创造力与想象力。
- 原则一：避免不必要的复杂性和过早优化，尤其不为非常边缘的场景提前设计。不为已经废弃的流程增加兼容层。
- 原则二：价值导向，以用户实际能完成的任务和体验判断功能价值。
- 原则三：坚持辩证思考，区分事实、假设与方案取舍。
- 原则四：从根源修复问题，不打症状补丁。接口、配置或代码删除后，相关调用和文档应同步转向新方式。
- 原则五：以明确职责和通用契约实现复用，避免为单一组件污染公共模型。

## 产品定位与范围

- 定位为可解释、可比较、可控执行的多信号交易工作台。
- 当前主体是一套策略配置、多个信号组件、多个账户执行，以及人工可控的研究反馈。
- 保持模块化单体。没有明确业务需求前，不建设微服务、插件市场、工作流 DSL、多策略组合优化或自动调权系统。

## 主体设计

基本流程：行情上下文 → 信号组件 → 综合观点 → 策略目标 → 资金池风控与可选审批 → 差额执行 → 事实记录与评估。

### 信号组件提供可比较的市场观点

- Kronos、LLM 四智能体委员会和自定义信号来源处于同一级。委员会内部保留分析、辩论与汇总，对外是一份组件观点。
- 组件应明确标的、分析时点、预测期限、方向、评分、有效状态及依据。不同输入 K 线周期不等于不同预测期限；只有含义可比较的观点才适合直接融合。
- 组件不负责账户仓位、资金分配、审批或下单；仅分析流程不应依赖交易账户能力。
- 波动率等市场特征、硬性风险限制和订单执行算法各自归属相应层，不强行包装成方向投票。

### 融合表达综合观点

- 第一阶段保留确定性加权融合，保留各组件的原始输出和加权贡献，便于解释和复核。
- 用户配置的信任权重表示影响力，不等同于历史胜率、校准概率或资金分配比例。组件评分也不能未经验证就解释为成功概率。
- 综合观点应能区分方向强弱、组件分歧和有效参与情况。相反观点抵消、共同中性与全部弃权不能仅凭同一个零分被视为同一状态。
- 不默认增加外层 LLM 裁判，也不因组件数量增加就假定获得了更多独立证据。

### 策略决定目标，风控约束资金，执行落实仓位

- 从综合观点到目标仓位的映射是一项明确的策略规则；不得把置信度直接解释为可使用的资金比例。
- 策略负责仓位映射与退出规则，风控负责限制或拒绝，执行负责根据账户事实将目标转换为差额订单。
- 弃权、失败、中性观点、保持现有仓位和目标空仓应有明确区别，不能静默互相转换。信号无法使用时，已有风险保护不能因此失效。
- 退出策略明确拥有 ATR 等指标的计算周期和数据需求，不依赖组件排列顺序，也不与预测期限、评估窗口或调度频率混用。
- 在相同输入和规则下，实时与回测的目标计算、退出策略及风控语义应一致；撮合和数据可得性差异必须显式说明。
- 人工审批可配置，但审批和信号权重都不能绕过硬性风控或真实交易授权。

### 平台、连接与资金池分开

- 平台类型描述适配器能力；连接描述具体账户、凭据和环境；资金池描述账户分配、风险状态及审批范围。
- 模拟与真实环境由平台能力明确声明，账户和资金池必须遵守资金性质隔离。更换执行平台不应改变市场观点，但可以改变可执行数量与成交结果。
- 资金池目前用于同一套策略的多账户执行，不把账户数量增加等同于策略数量增加。
- 资金池是逻辑执行与风险容器，不暗示跨平台资金已经汇集、自动转账或自动重新分配。
- 区分独立风险状态与独立风险参数；需要不同资金池风险预算或独立策略时，再明确设计其归属。

### 扩展和配置围绕已有契约

- 第一阶段新增信号组件、交易平台和行情源，通过后端代码实现并注册，不要求安装插件或扫描运行时包。
- 后端提供参数、凭据、环境、能力和展示元数据，前端使用通用表单与结果块。使用既有字段和展示类型的新组件或平台，不应要求前端增加类型专属分支。
- 全新交互或展示形态可以扩展前端公共能力，不承诺任意新类型都无需前端改动。
- 组件声明数据需求，采集层按需求提供数据，不因某个内置组件而强制所有组件采集相同辅助信息。
- 运行配置以数据库为唯一来源，支持网页动态调整；TOML 和环境变量仅保留必要的引导配置，不为旧配置方式增加回退链。
- 配置页面使用中文标签、说明和结构化控件，不向用户暴露参数 JSON。凭据单独安全保存，不进入可读配置快照或日志。
- 保存配置、检查连接、启用自动运行和授权真实交易是不同动作，界面与后端都应明确其状态和副作用。

### 研究反馈形成可核验的证据

- 决策时的输入、配置版本、组件观点和执行依据应可追溯；事后观察与原始预测分开保存。
- 比较组件时使用明确的预测期限、共同样本和评估条件，区分方向命中、预测误差与扣除成本后的交易表现。
- 显式记录缺失或替代数据及模型调用证据，不将占位数据、流程测试或模拟成交描述为真实模型效果、真实平台验收或盈利证明。
- 先比较组件单独运行与融合运行的增益，由用户决定是否调整权重和参数。自动学习与自动调权不默认进入交易主链。

## 工作与验收约束

- 分析和审查请求默认只读。修改运行数据库、调用付费模型、启用交易、提交或推送应具有对应的明确授权。
- 保留用户已有改动，不顺带清理无关文件。原则文档不构成执行交易或扩大重构范围的授权。
- 验收优先覆盖主流程和核心契约：组件换序不改变业务含义；弃权不混同空仓；只读分析不触碰执行；实时与回测使用一致策略规则。
- 修改架构、接口、配置或页面入口时同步相关文档，避免旧说明继续指导新流程。

## 命令约定

所有 shell 命令使用 `rtk` 前缀，需要原始输出时使用 `rtk proxy`。

@/Users/rccpony/.codex/RTK.md
