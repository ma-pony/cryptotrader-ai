# 后端 API 基础地址（与 `arena serve` 默认端口对齐）
VITE_API_BASE_URL=http://localhost:8003

# X-API-Key（生产环境必填；dev 模式 FastAPI 默认放行可留空）
VITE_API_KEY=

# OpenTelemetry collector 端点（可选，用于前端 web vitals 上报）
VITE_OTLP_UI_ENDPOINT=
