# 跨项目对比矩阵

**状态**：✅ Phase 2 完成 —— 8 列 × 8 项目 = 64 格全部填写。
**最后更新**：2026-05-08

每格格式：`<一句话发现>` + `path/to/file.ext`（指向源码引用）。N/A 格用 "N/A — <理由>"。

| 项目 | 进化算法 | 技能数据结构 | 检索机制 | 记忆 ↔ 技能 | 提示组装 | Evaluation | Agent ↔ Skill 边界 | 工程实现 |
|---|---|---|---|---|---|---|---|---|
| **SkillClaw** | 5 阶段监督式 LLM 批处理流水（Summarize → Session Judge → Aggregate → Execute → Skill Verifier）；每 10 分钟轮询；阈值 0.75 准入门控 · `evolve_server/pipeline/` | `SKILL.md` Markdown + YAML frontmatter（`name`/`description`/`keywords`/`version`/`parent`）；本地 + S3 共享存储；`history/vN.md + vN_evidence.md` 版本审计 · `skills/` | LLM 自身 `<load_skill>` 信号驱动（非 embedding）；`push_min_injections=5` 注入数量门控 · `api_proxy/skill_loader.py` | 因果链会话 DB 双用途：喂演化数据集 + 注入运行时上下文；Task Ledger 跨会话目标持久化 · `api_proxy/skill_router.py` | 两阶段：catalog YAML 在 system prompt 中常驻 → 命中后追加完整 SKILL.md body · `api_proxy/proxy.py` | LLM-as-judge 4 维加权（任务完成 55% / 决策质量 / 简洁 / 引用证据）；Skill Verifier 四项准入门控 · `evolve_server/judge.py` | Client Proxy 拦截 `/v1/chat/completions`；客户端 dashboard sync 拉取技能更新；Skill 共享池 cross-user · `api_proxy/proxy.py` | 离线 Evolve Server + 本地客户端 Proxy；本地 + S3 双存储；conservative 演化策略每次修改强制引用 session 证据 · `evolve_server/main.py` |
| **MetaClaw** | 双循环：快内循环（失败对话 LLM 提炼新技能）+ 慢外循环（用户空闲时 Tinker LoRA 梯度更新）；三路触发（成功率 < 0.4 / 每 10 轮 / session_done）· `evolution/loop.py` | 6 类 `MemoryUnit` dataclass（`type`/`content`/`importance`/`access_count`/`recency`/`keywords`/`embedding`/`confidence`）；分组渲染到 system 消息 · `memory/units.py` | **IDF 加权混合**（关键词 IDF + embedding 余弦 + importance + recency_bonus + reinforcement_score） × type_boost × confidence_factor；recency 连续衰减；Qwen3-0.6B embedding · `memory/retriever.py` | 6 类分组写到 system 消息单一段；快内循环存盘 + 慢外循环 LoRA 更新；generation 计数器追版本 · `memory/store.py` | 3 模式（Synergy / Memory-only / Skills-only）；20k token 贪心截断；分段 system 消息 · `api_server.py:build_prompt()` | 检索成功率 + 用户隐式反馈；批次成功率 < 0.4 触发演化；无显式回滚 · `evolution/triggers.py` | api_server.py 透明代理；OpenAI 兼容 API；演化 LLM 可用任意端点 · `api_server.py` | 双循环分离（快 prompt + 慢 LoRA）；MAML support/query 分离；无共识协议依赖 · `evolution/scheduler.py` |
| **OpenClaw-RL** | **GRPO 真 RL** + SLIME 训练后端；持续在线（推理同时训练）；turn-level loss masking；PRM Judge 三值打分多数投票 · `openclaw_rl/grpo.py`、`openclaw_rl/prm.py` | 无 SKILL 抽象；技能体现在策略权重 + 工具描述（≤500 字符）· `openclaw_rl/tools.py` | 经验注入到最后一条 user 消息；session-experience / replay / consolidate 三模式切换 · `openclaw_rl/oel.py` | in-memory dict（`_session_*`）；OEL `loop.create_task()` 异步抽取经验 · `openclaw_rl/sessions.py` | tokenizer-native via `tokenizer.apply_chat_template()` (Jinja2)；经验**仅注入最后一条 user 消息** · `openclaw_rl/utils/template.py` | PRM Judge 三值打分（+1/-1/0）多数投票；reward = next-state observable signal · `openclaw_rl/prm.py` | 4 异步循环（API Server / Rollout Worker / PRM Judge / Trainer）；推理-训练零协调 · `openclaw_rl/__main__.py` | **8 GPU 默认**（4 训练 + 2 推理 + 2 PRM）；SLIME 分布式后端；本机部署不可行 · README + `slurm_config.yaml` |
| **Hermes** | **GEPA**（遗传-帕累托提示进化）+ DSPy Signature 包装 + **Reflective Mutation**（LLM 自反反馈作变异输入）；35× 少 rollouts 超 GRPO；离线批量（$2-10/run）· `evolution/core/gepa.py` | `SKILL.md` YAML frontmatter（`name`/`description` 冻结）+ Markdown body（可演化）；body ≤15KB；提示段 ≤120% 当前长度 · `skills/<category>/<name>/SKILL.md` | `SESSION_SEARCH_GUIDANCE` 本身是**可演化 prompt 段** —— 检索触发逻辑也被 GEPA 优化 · `agent/prompt_builder.py` | `SessionDB` 双角色：喂优化数据集 + 注入运行时上下文；记忆检索逻辑可演化 · `agent/hermes_state.py` | 命名段（`MEMORY_GUIDANCE`/`SKILLS_GUIDANCE`/`SESSION_SEARCH_GUIDANCE`/`PLATFORM_HINTS`/`DEFAULT_AGENT_IDENTITY`）；部分 DSPy 可演进、部分固定；不会话中热替换 · `agent/prompt_builder.py` | **多目标 Pareto 前沿**：50% 正确率 + 30% 程序遵循 + 20% 简洁（含长度惩罚）；不超越则保留 · `evolution/core/fitness.py` | Hermes Agent（外部仓库）+ optimization pipeline（独立）；`batch_runner.py` + `trajectory.py` 解耦 · `evolution/runner/` | 离线批量（API 调用，无 GPU）；git 谱系追踪；Phase 4 Darwinian Evolver（AGPL v3 注意）· `PLAN.md` |
| **EvoSkill** | **纯数值 accuracy frontier 替换**（无 LLM-as-judge）；子程序 vs frontier 最差成员比较；`update_frontier()` True 则替换 · `evoskill/feedback_descent.py` | git 分支 + `program.yaml`（`parent_iter` / `score` / `created_at` / `frontier_status`）；可执行代码 · `evoskill/program_manager.py` | 选 frontier Top-N 作为 Proposer 输入；按 fitness rank 排序 · `evoskill/proposer.py` | `feedback_history.md` 即记忆；`update_frontier()` True 时清空（精确触发）；`related_iterations` 反向追溯 · `evoskill/feedback_descent.py` | Proposer 接收**三要素 + 三级截断**：失败样本 + 反馈历史 + 现有技能枚举；60k/20k/5k 字符级联截断 · `evoskill/proposer.py:build_proposer_query()` | 纯数值 accuracy 在验证集上比较；frontier 替换不胜出则不晋级 · `evoskill/evaluator.py` | sentient-agi SDK；Proposer / Generator / Evaluator / Frontier 四角色解耦 · `evoskill/__init__.py` | **git 谱系版本控制**：`program/iter-*` 分支 + `frontier/*` 标签 + `program.yaml`；零额外存储 · `evoskill/program_manager.py` |
| **EvoSkills** | **IDE/IVE/ESE 三机制**：成功后 IDE（向上抽象方向）+ ESE（向下蒸馏策略）；失败后 IVE（5 诊断问题分类 + 三连败永久归档）· `memory/evolution.py` | 13 个 SKILL.md（Markdown-first）+ frontmatter（`allowed_tools` 白名单 + `should-trigger` 路由）· `skills/*.md` | top-k 余弦相似度（Qwen embedding，k=5）+ 关键词路由 fallback · `memory/retriever.py` | 双存储：M_I（方向级）+ M_E（策略级）；top-k 余弦注入；跨周期更新 · `memory/store.py` | 13 个 SKILL.md 含 `allowed_tools` 白名单 + `should-trigger` 声明式路由；SKILL.md 即 prompt body · `agent/loader.py` | Elo 比赛 + 三连败永久归档；Single observation → Confirmed[N] → Contradicted 三态置信度 · `memory/evolution.py` | 无显式 agent 抽象；13 SKILL.md 即 prompt body；由科研流程驱动 · `agent/runner.py` | 纯 Markdown + prompt 驱动（无 Python）；无显式 agent 框架；目标域偏学术工作流 · `README.md` |
| **skill-evolution** | **7 步反思流程**（Step 3 影响扫描 + Step 4 确定性阶梯 + Step 6 静默漏触发信号）；2 次同问题→停止打补丁 / 3 次不同问题→标记重设计 · `docs/protocol.md` | 三层结构 L1（frontmatter ≤300 行）/ L2（SKILL.md body）/ L3（references/* 按需）；脚本/hooks 与 SKILL 分离 · `loader/progressive.py` | L1 关键词 → L2 触发 → L3 按需的渐进披露；声明式 trigger 字段匹配 · `loader/progressive.py` | 无独立记忆模块；反思直接写回 SKILL.md 或脚本；"确定性阶梯"分清 LLM/脚本职责 · `docs/protocol.md` | 三层渐进披露：L1 元数据始终在 context、L2 SKILL.md 触发时载入、L3 references/ 按需 · `loader/progressive.py` | **5 信号成熟度判定**：production-tested ≥3 / stable 5 次无修改 / well-structured / clean / self-contained；Step 6 人类确认门控 · `protocol.md` | Claude Code 元框架；7 步反思 + 确定性阶梯（LLM/脚本/hooks 职责） · `docs/protocol.md` | 反思流程纯 prompt + 脚本（无独立 daemon）；用户驱动改进；hook script 触发 · `hooks/reflect.sh` |
| **microsoft/autogen** | **N/A** — 纯编排框架，无技能演化概念；MAF 也无演化 · MAF README | `BaseTool` / `FunctionTool` / `Workbench` 三层；`ToolSchema` JSON Schema 自动生成；`list_tools()` 动态查询 · `python/packages/autogen-core/src/autogen_core/tools/` | `ListMemory`（全文注入用 SystemMessage）/ `ChromaDB`（向量 + score_threshold 用 UserMessage）；4 种 ChatCompletionContext（buffered / head-tail / token-trimmed / unlimited）· `python/packages/autogen-core/src/autogen_core/memory/` | `Memory.update_context()` 注入钩子；对话历史走 `ChatCompletionContext`（4 变体）· `python/packages/autogen-core/src/autogen_core/model_context/` | **四槽位分离**：`_system_messages` + `_tools` + `Memory.update_context()` + `ChatCompletionContext`；system prompt 不入 ChatCompletionContext（缓存友好）· `_assistant_agent.py` | `agbench`（Docker 隔离 + JSONL 任务定义）；`ToolCallEvent` 三元组格式 · `python/packages/agbench/` | `BaseChatAgent` / `AssistantAgent` / `Team`（3 层）；`SocietyOfMindAgent` 模式；增量消息协议；3 种 Team（GroupChat / SwarmTeam / SelectorGroupChat）· `python/packages/autogen-agentchat/src/autogen_agentchat/agents/` | 工业级（10 子包）；.NET 同步实现；MAF 迁移路径；部署量级远超其他项目 · `python/packages/`、`dotnet/` |

---

## 状态图例

- ✓ —— 该列已完成
- N/A — <理由> —— 该项目不具备此属性，附带原因
- 8 列 × 8 项目 = **64 格 / 64 已填**（autogen 进化算法 + 技能数据结构 / OpenClaw-RL 技能数据结构这 3 格是 N/A）

## 备注

- **autoresearch 替换**：autoresearch 按 D-PROC-01 退出活跃研究集（仅 2/8 列产生信号）；改为 microsoft/autogen Tier 2 深度研究。原 `autoresearch.md` 重命名为 `_deferred-autoresearch.md` 保留供参考。
- **autogen 贡献边界**：autogen 没有技能演化概念（纯编排框架），其价值集中在提示组装 + 记忆接线 + Agent ↔ Skill 边界（spec 017 + spec 018 边界设计相关）。其他 7 个项目承担演化算法的研究压力。
- **License 覆盖**：8 个活跃项目全部 permissive（5 MIT + 3 Apache-2.0），spec 018 fork-borrow 建议无 GPL 污染风险。**Hermes Phase 4 Darwinian Evolver 是 AGPL v3 — 不可借鉴代码，但算法设计可参考**。
- **GPU 需求总览**：OpenClaw-RL 默认需 8 GPU（不可行）；MetaClaw 慢外循环需 GPU（可降级）；其余 6 项目均无 GPU 需求 ✓ 适配本机部署。
